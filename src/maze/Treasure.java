package maze;

/**
 * The enum of Treasure which are allocated at location nodes which are collected by the player
 * when he is traversing through the dungeon.
 */
public enum Treasure {
  DIAMOND, RUBY, SAPPHIRE
}
